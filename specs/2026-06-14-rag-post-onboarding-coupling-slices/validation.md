# Validation

This slice is ready when the roadmap preserves the current active work while
also naming the next deferred `rag` decoupling candidates.

## Acceptance Checks

- The new spec bundle exists.
- The roadmap includes a deferred post-onboarding note for the remaining
  `rag` coupling candidates.
- The deferred list names:
  - `rag-document-canonicalization-seam-extraction`
  - `rag-markdown-chunk-normalization-seam-extraction`
  - `rag-arl-remuneration-domain-seam-extraction`
- The active `remaining in Phase 18` section is not expanded by those deferred
  candidates.
