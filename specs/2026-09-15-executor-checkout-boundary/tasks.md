# Executor and Checkout Boundary — Tasks

## Capture-Size Correction — 2026-09-17

Current bounded work is this new seven-document correction. All S1 rows below
are historical work packages, not renewed grants. Role `executor`, dispatch
owner `019f71d6-632c-7870-bfa2-89513fdeb85a`; principal checkout only.

| Task | Completion evidence |
|---|---|
| CAP.1 Diagnose | Exact physical/fingerprint preflight, canonical source search, bounded source-task receipt; ranked hypotheses and old-rule RED |
| CAP.2 Correct | S1-AC06, sole plan recipe/graph/table, workflow pointer, prunable lesson, and spec/validation coherence; seven-path allowlist |
| CAP.3 Verify | Literal pagination fixtures plus explicitly manual stop-table assessment; source/range/exit retention, scoped whitespace/links/diffs, twelve-delta postflight |
| CAP.4 Return | Sanitized diagnostic record and Capsule, exact final hashes, checks/skips/balances; READY_FOR_INDEPENDENT_REVIEW |

Current grant permits one initial edit pass plus two in-scope documentary
adjustments for expected inconsistencies, with hard totals of 160 calls and
200 pages. It permits no mutation retry, escalation, subagents, dispatch,
acceptance, Git mutation, providers, product tests, or readiness continuation.
The [plan amendment](plan.md#capture-size-amendment--2026-09-17) owns capture
semantics; these task rows do not duplicate its recipe or graph.

## Historical S1: Specification Only

Role `executor`; dispatch owner `019f71d6-632c-7870-bfa2-89513fdeb85a`;
principal checkout; route Astra/high. This is the already-received task, not a
dispatch request. The original decision map is accepted input; no additional
discovery interview is needed.

| Task | Allowed result | Verification / stop |
|---|---|---|
| S1.1 | Fresh physical identity and initial fingerprint | Exact twelve-delta grant, hashes/modes, raw index, marker, branch/HEAD, worktrees; mismatch STOP |
| S1.2 | Read skill/references and named repository sources | Bounded capture; required evidence missing after safe lookup STOP |
| S1.3 | Create spec/tasks and extend requirements/plan/validation | Five-path allowlist only; preserve history and all nine protected deltas |
| S1.4 | Verify documentary candidate | Links, whitespace, manual AC/graph/table/DoD, fourteen-delta postflight and preservation |
| S1.5 | Return Receipt Capsule and exact final manifest | Static evidence only; READY_FOR_OWNER_SPEC_DECISION |

One initial edit pass and at most two expected documentary adjustments are
allowed. Real failure, drift, unknown mutation, sensitive-data risk, or material
scope conflict terminates S1; no mutation retry or harness repair is included.
The current bounded read-only capture/search exception changes no future grant.
Completion evidence belongs to the terminal capsule, not checked boxes that
pretend future work has run.

## Future Work: Not Authorized by S1

| Proposed action | Required fresh boundary | Scope and evidence to bind before dispatch |
|---|---|---|
| Owner specification decision | Owner accepts/rejects exact five-document candidate | No implied delivery, review, Git, or readiness authority |
| Operational materialization | New visible delivery task under a named grant | Update `docs/agents/executor-workflow.md` and `tasks/lessons.md` before review; any AGENTS/master pointer changes must be separately allowlisted; freeze literal inventory body, manifest, read bounds and selected checks |
| Independent review | New visible task that did not author the candidate | FULL initially or on new boundary/topology; exact candidate, read-only review commands and receipt |
| C1 or C2 correction | New visible task for each exact preauthorized correction | Expected findings only, remaining semantic cycle and action budgets, exact paths and successor fingerprint; fresh review afterward |
| Owner candidate acceptance | Explicit owner decision after review | Bytes and residual risks; acceptance is not a successor grant |
| Git action | Separately named authority and visible task | Current fixed point, exact action and scope; no mutating command is provided here |
| Operational-readiness continuation | New owner grant and fresh visible task | Existing readiness work remains preserved; select its next evidence rung separately |
| Provider or external action | Additional separately named authority | Exact data, contracts, cost limits, safety and human-review bounds |

The complete transition guards and recipe live only in [plan.md](plan.md).
The table above identifies work packages, not a second executable graph.
Neither master nor executor may infer these grants from a spec, task row,
PASS, receipt, acceptance, or Git result. No successor is created by S1.

## Ticketing Decision and Unresolved Inputs

`$to-tickets` is not needed now: the current five-file package and future
authority boundaries are already explicit, and no configured tracker target
was identified. Creating tickets would add no missing acceptance criterion or
risk control. If the owner later authorizes several independent delivery units,
ticket decomposition can be decided then, without activating these rows.

There is no unresolved semantic choice blocking S1. Future task IDs, exact
fingerprints, literal inventory body, path lists, read limits, action budgets,
and validation invocations are dispatch inputs to bind later. They are not
standing authority, an excuse to broaden this task, or new interview questions.
