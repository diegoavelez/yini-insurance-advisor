# Validation

This slice is ready when the repository documents a stricter onboarding-slice
policy without changing historical implementation truth.

## Acceptance Checks

- The new spec bundle exists.
- The playbook now prefers one bundle per root-cause family rather than one
  bundle per query-sized failure.
- The playbook explicitly says scenario variants belong in `validation.md`
  unless the root-cause family changes.
- The roadmap contains a compact category rollup view and reframes the long
  slug lists as traceability indexes.
- The existing category status remains unchanged.
